/** TODO(B2 Pranav): check-in/out card + today grid (admin view) */
export default function AttendancePage() {
  return <main><p className="text-slate-400">attendance — owner: B2 (Pranav)</p></main>;
}
