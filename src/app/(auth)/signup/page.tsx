/** TODO(Pranav): company signup — signupSchema → create company row (service role or RPC), auth.signUp, insert own profile as admin, redirect /dashboard */
export default function SignupPage() {
  return <main className="grid min-h-screen place-items-center"><p className="text-slate-400">signup — owner: Pranav</p></main>;
}
