/** TODO(B1 md): admin approval queue — pending leave_requests with approve/reject; RLS admin policy gates this */
export default function ApprovalsPage() {
  return <main><p className="text-slate-400">approvals — owner: B1 (md)</p></main>;
}
