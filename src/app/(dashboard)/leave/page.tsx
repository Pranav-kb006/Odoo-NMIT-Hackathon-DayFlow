/** TODO(B2): apply form (leaveApplySchema) + my-leaves list; admin sees team queue with approve/reject */
export default function LeavePage() {
  return <main><p className="text-slate-400">leave — owner: B2</p></main>;
}
