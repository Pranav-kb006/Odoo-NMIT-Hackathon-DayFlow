/** TODO(B3 Nithin): profile detail — info card + attendance history + leave history tabs */
export default function EmployeeProfilePage() {
  return <main><p className="text-slate-400">employee profile — owner: B3 (Nithin)</p></main>;
}
