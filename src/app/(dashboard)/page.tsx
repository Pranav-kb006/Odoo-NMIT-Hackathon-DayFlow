/** TODO(B4): role-aware home — admin: stat cards + trend + approvals shortcut; employee: today status + my leave summary */
export default function DashboardHome() {
  return <main><p className="text-slate-400">dashboard home — owner: B4</p></main>;
}
