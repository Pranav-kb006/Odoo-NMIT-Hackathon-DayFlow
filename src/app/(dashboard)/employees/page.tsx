/** TODO(B3): employee table — search, filter by department/status, add/edit modal, CSV import/export */
export default function EmployeesPage() {
  return <main><p className="text-slate-400">employees — owner: B3</p></main>;
}
